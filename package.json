{
  "name": "mcpwatch",
  "version": "0.1.0",
  "private": false,
  "description": "Public MCP and agent-tool directory, leaderboard, CLI, and static SEO site.",
  "type": "module",
  "files": [
    "dist-cli",
    "data/latest",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "agents",
    "leaderboard",
    "cli"
  ],
  "homepage": "https://github.com/rogerchappel/mcpwatch#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/mcpwatch.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/mcpwatch/issues"
  },
  "bin": {
    "mcpwatch": "dist-cli/cli.js"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build:cli": "tsc -p tsconfig.cli.json && node scripts/fix-cli-shebang.cjs",
    "build:site": "tsx scripts/build-site.ts --input data/latest/servers.json --out site",
    "refresh": "tsx scripts/refresh.ts --out data/latest/servers.json --history data/history",
    "validate:data": "tsx scripts/validate-data.ts data/latest/servers.json",
    "smoke:cli": "pnpm run build:cli && node dist-cli/cli.js search filesystem --input fixtures/servers.json && node dist-cli/cli.js top --input fixtures/servers.json --limit 2 && node dist-cli/cli.js diff --before fixtures/servers-before.json --after fixtures/servers.json",
    "test": "pnpm run check && pnpm run validate:data && pnpm run build:site && pnpm run smoke:cli"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
